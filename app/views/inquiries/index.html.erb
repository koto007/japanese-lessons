<h1 class="text-center">Tous les messages</h1>
<table class="table table-striped table-bordered">
    <thead>
      <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Message</th>
        <th>Envoyé</th>
      </tr>
    </thead>
    <tbody>
      <% @inquiries.each do |inquiry| %>
        <tr>
          <td><%= inquiry.name %></td>
          <td><%= inquiry.email %></td>
          <td><%= inquiry.message %></td>
          <td><%= inquiry.created_at %></td>
        </tr>
      <% end %>
    </tbody>
</table>


  